#version 320 es

precision highp float;

layout(local_size_x = 16, local_size_y = 16, local_size_z = 1) in;

layout(rgba8ui) writeonly uniform highp image2D _group_0_binding_0;


void main() {
    uvec3 global_ix = gl_GlobalInvocationID;
    imageStore(_group_0_binding_0, ivec2(int(global_ix[0]), int(global_ix[1])), vec4(1.0, 0.0, 0.0, 1.0));
    return;
}

